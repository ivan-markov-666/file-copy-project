{
  "name": "directory-scanner",
  "version": "2.0.0",
  "description": "Recursively scans a directory and outputs the paths and contents of files, with optional comment stripping",
  "main": "dist/scanner.js",
  "scripts": {
    "scanner": "ts-node src/scanner.ts",
    "build": "tsc",
    "start": "node dist/scanner.js"
  },
  "dependencies": {
    "decomment": "^0.9.5",
    "strip-json-comments": "^5.0.1"
  },
  "devDependencies": {
    "@types/node": "^18.15.0",
    "@types/decomment": "^0.9.0",
    "ts-node": "^10.9.1",
    "typescript": "^4.9.5"
  }
}