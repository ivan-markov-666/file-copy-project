AI-PROMPTS-EXAMPLES.md
# 🤖 AI Prompt Примери за Генериране на Тестове

Този файл съдържа примери за AI prompts, които могат да генерират качествени API тестове.

---

## 📋 Prompt Template (Основен)

```
Генерирай Supertest + Jest тестове за следното API:

BASE URL: [URL]
ENDPOINT: [endpoint path]

HTTP Методи:
- GET: [описание]
- POST: [описание + required fields]
- PUT: [описание]
- DELETE: [описание]

Валидации:
- [какво трябва да се провери]
- [response structure]
- [error cases]

Използвай template от файл tests/template.test.js
```

---

## 🎯 Пример 1: CRUD за Users

### Prompt:
```
Генерирай Supertest + Jest тестове за Users API:

BASE URL: https://api.example.com
ENDPOINT: /users

HTTP Методи:
- GET /users - връща списък с всички потребители
- GET /users/:id - връща един потребител по ID
- POST /users - създава нов потребител
  Required fields: name (string), email (string), age (number)
- PUT /users/:id - обновява съществуващ потребител
- DELETE /users/:id - изтрива потребител

Валидации:
- Status codes: 200 (success), 201 (created), 404 (not found), 400 (bad request)
- Response structure трябва да съдържа: id, name, email, age, createdAt
- POST трябва да валидира email format
- PUT трябва да върне 404 за несъществуващ user
- DELETE трябва да върне 404 за несъществуващ user

Error cases:
- POST без required fields трябва да върне 400
- GET с невалидно ID трябва да върне 404
- POST с невалиден email format трябва да върне 400
```

### Очакван Output:
```javascript
const request = require('supertest');

const baseURL = 'https://api.example.com';

describe('Users API Tests', () => {
  
  describe('GET /users', () => {
    test('should return all users', async () => {
      const response = await request(baseURL)
        .get('/users')
        .expect(200);
      
      expect(Array.isArray(response.body)).toBe(true);
    });
  });
  
  describe('GET /users/:id', () => {
    test('should return user by id', async () => {
      const response = await request(baseURL)
        .get('/users/1')
        .expect(200);
      
      expect(response.body).toHaveProperty('id', 1);
      expect(response.body).toHaveProperty('name');
      expect(response.body).toHaveProperty('email');
      expect(response.body).toHaveProperty('age');
    });
    
    test('should return 404 for non-existent user', async () => {
      await request(baseURL)
        .get('/users/99999')
        .expect(404);
    });
  });
  
  describe('POST /users', () => {
    test('should create new user', async () => {
      const newUser = {
        name: 'John Doe',
        email: 'john@example.com',
        age: 25
      };
      
      const response = await request(baseURL)
        .post('/users')
        .send(newUser)
        .expect(201);
      
      expect(response.body).toHaveProperty('id');
      expect(response.body.name).toBe(newUser.name);
      expect(response.body.email).toBe(newUser.email);
      expect(response.body.age).toBe(newUser.age);
    });
    
    test('should return 400 when required fields missing', async () => {
      const invalidUser = { name: 'John' };
      
      await request(baseURL)
        .post('/users')
        .send(invalidUser)
        .expect(400);
    });
    
    test('should return 400 for invalid email format', async () => {
      const invalidUser = {
        name: 'John Doe',
        email: 'invalid-email',
        age: 25
      };
      
      await request(baseURL)
        .post('/users')
        .send(invalidUser)
        .expect(400);
    });
  });
  
  // ... PUT и DELETE тестове
});
```

---

## 🎯 Пример 2: Authentication API

### Prompt:
```
Генерирай Supertest + Jest тестове за Authentication API:

BASE URL: https://api.example.com
ENDPOINTS:
- POST /auth/register - регистрация на нов потребител
  Required: username, email, password
- POST /auth/login - логин на потребител
  Required: email, password
  Returns: token
- GET /auth/me - връща данни за текущия потребител (изисква token)

Валидации:
- Register трябва да върне 201 и user object
- Login трябва да върне 200 и JWT token
- GET /auth/me без token трябва да върне 401
- GET /auth/me с невалиден token трябва да върне 403
- Login с грешна парола трябва да върне 401
```

---

## 🎯 Пример 3: E-commerce Products API

### Prompt:
```
Генерирай Supertest + Jest тестове за Products API:

BASE URL: https://api.shop.com
ENDPOINTS:
- GET /products - списък продукти (поддържа pagination и filtering)
  Query params: page, limit, category, minPrice, maxPrice
- GET /products/:id - един продукт
- POST /products - създава продукт (изисква admin token)
  Required: name, description, price, category, stock
- PATCH /products/:id/stock - обновява stock (изисква admin token)

Валидации:
- GET /products трябва да поддържа pagination
- Query params за filtering трябва да работят правилно
- POST без admin token трябва да върне 403
- PATCH stock с отрицателно число трябва да върне 400
- Response structure: id, name, description, price, category, stock, createdAt
```

---

## 🎯 Пример 4: File Upload API

### Prompt:
```
Генерирай Supertest + Jest тестове за File Upload API:

BASE URL: https://api.example.com
ENDPOINT: POST /upload

Functionality:
- Приема file upload (multipart/form-data)
- Поддържа само .jpg, .png, .pdf
- Max file size: 5MB
- Връща file URL и metadata

Валидации:
- Успешен upload трябва да върне 201 с fileUrl и fileId
- Unsupported file type трябва да върне 400
- File > 5MB трябва да върне 413 (Payload Too Large)
- Request без file трябва да върне 400
```

---

## 💡 Съвети за добри AI Prompts

### ✅ Добре структуриран prompt:
```
1. Ясно описан endpoint и functionality
2. Списък на всички HTTP методи
3. Required/Optional fields
4. Очаквани status codes
5. Response structure
6. Edge cases и error scenarios
```

### ❌ Лош prompt:
```
"Направи ми тестове за user API"
```
*(Твърде общо, липсва информация)*

### ✅ Добър prompt:
```
"Генерирай Supertest тестове за:
- GET /users (pagination: page, limit)
- POST /users (required: name, email)
- Валидирай response structure
- Тествай error cases (400, 404)"
```

---

## 🔄 Итеративно подобряване

### Първи опит (basic):
```
AI prompt: "Генерирай GET /users тест"

Резултат: Основен тест без валидации
```

### Втори опит (detailed):
```
AI prompt: "Генерирай GET /users тест с:
- Pagination validation
- Response structure check
- Array length validation"

Резултат: По-добър тест с повече assertions
```

### Трети опит (comprehensive):
```
AI prompt: "Генерирай GET /users тест suite с:
- Happy path (200)
- Empty results (200, empty array)
- Pagination (query params)
- Filtering (query params)
- Invalid parameters (400)
- Response time check"

Резултат: Пълен test suite
```

---

## 🎨 Custom Requirements Prompt

```
Генерирай Supertest + Jest тестове с:

1. Custom base URL от environment variable
2. Before/After hooks за setup/cleanup
3. Helper function за test data generation
4. Response time validation (<500ms)
5. Custom error messages
6. Logging на request/response

API Details:
[твоите детайли тук]
```

---

## 📊 Prompt за API Documentation Testing

```
Имам Swagger/OpenAPI spec файл. Генерирай тестове за:

Endpoints от spec:
- [copy/paste от swagger]

Изисквания:
- Тествай всички endpoints от spec
- Валидирай всички response schemas
- Провери всички required fields
- Тествай всички enum values
- Генерирай edge case тестове
```

---

## 🚀 Ready-to-use Prompts

### За бърза демонстрация:
```
Генерирай 5 прости Supertest теста за JSONPlaceholder API (https://jsonplaceholder.typicode.com):
- GET /posts
- GET /posts/1
- POST /posts
- PUT /posts/1
- DELETE /posts/1
```

### За production-ready тестове:
```
Генерирай comprehensive Supertest test suite за:
- API: [URL]
- Authentication: Bearer token
- All CRUD operations
- Error handling
- Data validation
- Performance checks
- Security tests
```

---

**Използвай тези prompts като база и ги адаптирай според твоето API!** 🎯


jest.config.js
module.exports = {
  testEnvironment: 'node',
  testMatch: ['**/tests/**/!(*template).test.js'],
  collectCoverageFrom: ['tests/**/!(*template).js'],
  coverageDirectory: 'coverage',
  verbose: true
};


package.json
{
  "name": "api-testing-basic",
  "version": "1.0.0",
  "description": "Basic API testing with Supertest + Jest",
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage"
  },
  "keywords": ["api", "testing", "supertest", "jest"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "jest": "^29.7.0",
    "supertest": "^6.3.3"
  }
}


README.md
# API Testing - Basic (Supertest + Jest)

Прост проект за автоматизирано API тестване с **Supertest** и **Jest**.  
Подходящ за демонстрация на AI генерирани тестове.

---

## 📦 Инсталация

```bash
# Клонирай/копирай проекта
cd api-testing-basic

# Инсталирай dependencies
npm install
```

---

## 🚀 Стартиране на тестовете

```bash
# Изпълни всички тестове
npm test

# Изпълни тестовете в watch mode (автоматично рестартира при промени)
npm run test:watch

# Генерирай coverage report
npm run test:coverage
```

---

## 📁 Структура на проекта

```
api-testing-basic/
├── tests/
│   ├── api-examples.test.js    # Примерни тестове (работещи с JSONPlaceholder API)
│   └── template.test.js        # Template за нови тестове
├── jest.config.js              # Jest конфигурация
├── package.json                # NPM dependencies и scripts
└── README.md                   # Тази документация
```

---

## 📝 Как да използваме Template-а

### Стъпка 1: Копирай template.test.js
```bash
cp tests/template.test.js tests/my-new-feature.test.js
```

### Стъпка 2: Редактирай файла
```javascript
const baseURL = 'https://your-api.com'; // Смени URL-а

describe('My Feature Tests', () => {
  test('should do something', async () => {
    const response = await request(baseURL)
      .get('/my-endpoint')
      .expect(200);
    
    expect(response.body).toBeDefined();
  });
});
```

### Стъпка 3: Изпълни тестовете
```bash
npm test
```

---

## 🤖 Примери за AI генериране

### Пример 1: Базов GET request
```javascript
test('should get user by ID', async () => {
  const response = await request(baseURL)
    .get('/users/1')
    .expect(200);
  
  expect(response.body).toHaveProperty('id', 1);
  expect(response.body).toHaveProperty('name');
});
```

### Пример 2: POST с валидация
```javascript
test('should create new user', async () => {
  const newUser = {
    name: 'John Doe',
    email: 'john@example.com'
  };
  
  const response = await request(baseURL)
    .post('/users')
    .send(newUser)
    .expect(201);
  
  expect(response.body.name).toBe(newUser.name);
  expect(response.body.email).toBe(newUser.email);
});
```

### Пример 3: Query параметри
```javascript
test('should filter users by role', async () => {
  const response = await request(baseURL)
    .get('/users')
    .query({ role: 'admin' })
    .expect(200);
  
  response.body.forEach(user => {
    expect(user.role).toBe('admin');
  });
});
```

### Пример 4: Headers и Authentication
```javascript
test('should authenticate with token', async () => {
  const response = await request(baseURL)
    .get('/protected-resource')
    .set('Authorization', 'Bearer YOUR_TOKEN')
    .expect(200);
});
```

---

## 🎯 Основни Supertest методи

| Метод | Описание | Пример |
|-------|----------|--------|
| `.get(url)` | GET request | `request(baseURL).get('/users')` |
| `.post(url)` | POST request | `request(baseURL).post('/users')` |
| `.put(url)` | PUT request | `request(baseURL).put('/users/1')` |
| `.patch(url)` | PATCH request | `request(baseURL).patch('/users/1')` |
| `.delete(url)` | DELETE request | `request(baseURL).delete('/users/1')` |
| `.send(data)` | Изпрати body data | `.send({ name: 'John' })` |
| `.query(params)` | Query параметри | `.query({ page: 1 })` |
| `.set(header, value)` | Добави header | `.set('Authorization', 'Bearer token')` |
| `.expect(status)` | Очакван status code | `.expect(200)` |
| `.expect(header, value)` | Очакван header | `.expect('Content-Type', /json/)` |

---

## 🎯 Основни Jest assertions

```javascript
// Equality
expect(value).toBe(expected);                    // Стриктно равенство (===)
expect(value).toEqual(expected);                 // Deep equality за обекти

// Truthiness
expect(value).toBeTruthy();
expect(value).toBeFalsy();
expect(value).toBeDefined();
expect(value).toBeNull();

// Numbers
expect(value).toBeGreaterThan(3);
expect(value).toBeGreaterThanOrEqual(3);
expect(value).toBeLessThan(5);
expect(value).toBeLessThanOrEqual(5);

// Strings
expect(string).toMatch(/pattern/);
expect(string).toContain('substring');

// Arrays
expect(array).toContain(item);
expect(array.length).toBe(5);

// Objects
expect(obj).toHaveProperty('key');
expect(obj).toHaveProperty('key', value);
```

---

## 🔧 Използване с различни APIs

### JSONPlaceholder (Тестово API - вградено в примерите)
```javascript
const baseURL = 'https://jsonplaceholder.typicode.com';
```

### Друго публично API
```javascript
const baseURL = 'https://api.github.com';
```

### Локално API
```javascript
const baseURL = 'http://localhost:3000';
```

---

## 💡 Съвети за AI генериране

### Добър prompt за AI:
```
Генерирай Supertest + Jest тестове за API endpoint /users с:
- GET /users - връща списък потребители
- GET /users/:id - връща един потребител
- POST /users - създава нов потребител (изисква name и email)
- PUT /users/:id - обновява потребител
- DELETE /users/:id - изтрива потребител

Включи валидации за:
- Статус кодове (200, 201, 404, 400)
- Response structure
- Задължителни полета
```

### AI ще генерира:
- Правилна структура с `describe` и `test`
- Всички HTTP методи
- Валидации с `expect`
- Error handling тестове

---

## 📊 Coverage Report

След `npm run test:coverage`:
```
--------------------|---------|----------|---------|---------|
File                | % Stmts | % Branch | % Funcs | % Lines |
--------------------|---------|----------|---------|---------|
All files           |     100 |      100 |     100 |     100 |
 api-examples.test  |     100 |      100 |     100 |     100 |
--------------------|---------|----------|---------|---------|
```

---

## 🆚 Разлика спрямо Playwright

| Feature | Supertest + Jest | Playwright |
|---------|-----------------|------------|
| **Цел** | API тестване | UI + API тестване |
| **Сложност** | ⭐ Просто | ⭐⭐⭐ Сложно |
| **Setup** | Минимален | TypeScript, browser setup |
| **Learning curve** | Лесно | По-трудно |
| **Скорост** | Много бързо | По-бавно (browser) |
| **AI friendly** | ✅ Много | ✅ Средно |

---

## 🎓 Какво е следващото?

След като разбереш основите:
1. ✅ Добави custom helpers
2. ✅ Създай fixtures за тестови данни
3. ✅ Интегрирай с CI/CD
4. ✅ Добави environment variables

---

## 📞 Полезни ресурси

- [Supertest Docs](https://github.com/ladjs/supertest)
- [Jest Docs](https://jestjs.io/docs/getting-started)
- [JSONPlaceholder API](https://jsonplaceholder.typicode.com/)

---

**Готово за демонстрация!** 🚀


tests
tests/template.test.js
const request = require('supertest');

// ========================================
// API TESTING TEMPLATE
// ========================================
// This file can be used as a template
// for generating new tests with AI
// ========================================

const baseURL = 'YOUR_API_BASE_URL_HERE'; // Replace with actual API URL

describe('API Test Suite - [FEATURE NAME]', () => {
  
  // ========================================
  // SETUP & TEARDOWN (if needed)
  // ========================================
  
  beforeAll(() => {
    // Runs once before all tests
    console.log('Starting test suite...');
  });
  
  beforeEach(() => {
    // Runs before each test
  });
  
  afterEach(() => {
    // Runs after each test
  });
  
  afterAll(() => {
    // Runs once after all tests
    console.log('Test suite completed.');
  });
  
  // ========================================
  // TEMPLATE: GET REQUEST
  // ========================================
  
  describe('GET /endpoint', () => {
    
    test('should return [expected result]', async () => {
      const response = await request(baseURL)
        .get('/your-endpoint')
        .expect(200);
      
      // Assertions
      expect(response.body).toBeDefined();
      // Add specific checks here
    });
    
    test('should handle query parameters', async () => {
      const response = await request(baseURL)
        .get('/your-endpoint')
        .query({ param1: 'value1', param2: 'value2' })
        .expect(200);
      
      // Assertions
    });
    
    test('should return 404 for invalid ID', async () => {
      await request(baseURL)
        .get('/your-endpoint/invalid-id')
        .expect(404);
    });
    
  });
  
  // ========================================
  // TEMPLATE: POST REQUEST
  // ========================================
  
  describe('POST /endpoint', () => {
    
    test('should create new resource', async () => {
      const newResource = {
        field1: 'value1',
        field2: 'value2'
        // Добави всички необходими полета
      };
      
      const response = await request(baseURL)
        .post('/your-endpoint')
        .send(newResource)
        .expect(201);
      
      // Assertions
      expect(response.body).toHaveProperty('id');
      expect(response.body.field1).toBe(newResource.field1);
    });
    
    test('should validate required fields', async () => {
      const invalidResource = {
        // Липсват задължителни полета
      };
      
      const response = await request(baseURL)
        .post('/your-endpoint')
        .send(invalidResource)
        .expect(400);
      
      // Проверка за грешки
      expect(response.body).toHaveProperty('error');
    });
    
    test('should handle duplicate entries', async () => {
      const resource = { uniqueField: 'duplicate-value' };
      
      // Създай първи път
      await request(baseURL)
        .post('/your-endpoint')
        .send(resource)
        .expect(201);
      
      // Опитай да създадеш същото отново
      await request(baseURL)
        .post('/your-endpoint')
        .send(resource)
        .expect(409); // Conflict
    });
    
  });
  
  // ========================================
  // TEMPLATE: PUT REQUEST
  // ========================================
  
  describe('PUT /endpoint/:id', () => {
    
    test('should update existing resource', async () => {
      const updatedResource = {
        id: 1,
        field1: 'updated-value1',
        field2: 'updated-value2'
      };
      
      const response = await request(baseURL)
        .put('/your-endpoint/1')
        .send(updatedResource)
        .expect(200);
      
      // Assertions
      expect(response.body.field1).toBe(updatedResource.field1);
    });
    
    test('should return 404 for non-existent resource', async () => {
      const updatedResource = { field1: 'value' };
      
      await request(baseURL)
        .put('/your-endpoint/99999')
        .send(updatedResource)
        .expect(404);
    });
    
  });
  
  // ========================================
  // TEMPLATE: PATCH REQUEST
  // ========================================
  
  describe('PATCH /endpoint/:id', () => {
    
    test('should partially update resource', async () => {
      const partialUpdate = {
        field1: 'new-value'
        // Само полетата, които искаме да обновим
      };
      
      const response = await request(baseURL)
        .patch('/your-endpoint/1')
        .send(partialUpdate)
        .expect(200);
      
      // Assertions
      expect(response.body.field1).toBe(partialUpdate.field1);
    });
    
  });
  
  // ========================================
  // TEMPLATE: DELETE REQUEST
  // ========================================
  
  describe('DELETE /endpoint/:id', () => {
    
    test('should delete resource', async () => {
      await request(baseURL)
        .delete('/your-endpoint/1')
        .expect(200);
      
      // Опционално: Провери че ресурса наистина е изтрит
      await request(baseURL)
        .get('/your-endpoint/1')
        .expect(404);
    });
    
    test('should return 404 for non-existent resource', async () => {
      await request(baseURL)
        .delete('/your-endpoint/99999')
        .expect(404);
    });
    
  });
  
  // ========================================
  // TEMPLATE: AUTHENTICATION TESTS
  // ========================================
  
  describe('Authentication', () => {
    
    test('should require authentication', async () => {
      await request(baseURL)
        .get('/protected-endpoint')
        .expect(401);
    });
    
    test('should accept valid token', async () => {
      const token = 'your-valid-token';
      
      const response = await request(baseURL)
        .get('/protected-endpoint')
        .set('Authorization', `Bearer ${token}`)
        .expect(200);
      
      // Assertions
    });
    
    test('should reject invalid token', async () => {
      await request(baseURL)
        .get('/protected-endpoint')
        .set('Authorization', 'Bearer invalid-token')
        .expect(403);
    });
    
  });
  
  // ========================================
  // TEMPLATE: ERROR HANDLING
  // ========================================
  
  describe('Error Handling', () => {
    
    test('should return proper error for bad request', async () => {
      const response = await request(baseURL)
        .post('/your-endpoint')
        .send({ invalid: 'data' })
        .expect(400);
      
      expect(response.body).toHaveProperty('error');
      expect(response.body.error).toHaveProperty('message');
    });
    
    test('should handle server errors gracefully', async () => {
      // Симулирай сценарий, който причинява server error
      const response = await request(baseURL)
        .post('/your-endpoint')
        .send({ triggerError: true })
        .expect(500);
      
      expect(response.body).toHaveProperty('error');
    });
    
  });
  
  // ========================================
  // TEMPLATE: RESPONSE VALIDATION
  // ========================================
  
  describe('Response Validation', () => {
    
    test('should return correct response structure', async () => {
      const response = await request(baseURL)
        .get('/your-endpoint')
        .expect(200)
        .expect('Content-Type', /json/);
      
      const data = response.body;
      
      // Проверка на структурата
      expect(data).toHaveProperty('field1');
      expect(data).toHaveProperty('field2');
      
      // Проверка на типовете
      expect(typeof data.field1).toBe('string');
      expect(typeof data.field2).toBe('number');
      
      // Проверка на стойностите
      expect(data.field1.length).toBeGreaterThan(0);
      expect(data.field2).toBeGreaterThanOrEqual(0);
    });
    
    test('should validate array responses', async () => {
      const response = await request(baseURL)
        .get('/your-endpoint/list')
        .expect(200);
      
      const data = response.body;
      
      expect(Array.isArray(data)).toBe(true);
      expect(data.length).toBeGreaterThan(0);
      
      // Провери всеки елемент
      data.forEach(item => {
        expect(item).toHaveProperty('id');
        expect(item).toHaveProperty('name');
      });
    });
    
  });
  
  // ========================================
  // TEMPLATE: PAGINATION
  // ========================================
  
  describe('Pagination', () => {
    
    test('should support pagination parameters', async () => {
      const response = await request(baseURL)
        .get('/your-endpoint')
        .query({ page: 1, limit: 10 })
        .expect(200);
      
      expect(response.body).toHaveProperty('data');
      expect(response.body).toHaveProperty('pagination');
      expect(response.body.data.length).toBeLessThanOrEqual(10);
    });
    
  });
  
  // ========================================
  // TEMPLATE: FILTERING & SORTING
  // ========================================
  
  describe('Filtering and Sorting', () => {
    
    test('should filter results', async () => {
      const response = await request(baseURL)
        .get('/your-endpoint')
        .query({ filter: 'active', category: 'test' })
        .expect(200);
      
      response.body.forEach(item => {
        expect(item.status).toBe('active');
        expect(item.category).toBe('test');
      });
    });
    
    test('should sort results', async () => {
      const response = await request(baseURL)
        .get('/your-endpoint')
        .query({ sortBy: 'name', order: 'asc' })
        .expect(200);
      
      // Провери че резултатите са сортирани
      for (let i = 1; i < response.body.length; i++) {
        expect(response.body[i].name >= response.body[i-1].name).toBe(true);
      }
    });
    
  });
  
});

// ========================================
// HELPER FUNCTIONS (optional)
// ========================================

// Function to generate test data
function generateTestData() {
  return {
    field1: `test-${Date.now()}`,
    field2: Math.floor(Math.random() * 100)
  };
}

// Function for test cleanup
async function cleanup(baseURL, id) {
  await request(baseURL)
    .delete(`/your-endpoint/${id}`)
    .expect(200);
}


tests/api-examples.test.js
const request = require('supertest');

// Използваме JSONPlaceholder като тестово API
const baseURL = 'https://jsonplaceholder.typicode.com';

describe('API Tests - Basic Examples', () => {
  
  // ========================================
  // GET REQUEST EXAMPLES
  // ========================================
  
  describe('GET Requests', () => {
    
    test('should get all posts', async () => {
      const response = await request(baseURL)
        .get('/posts')
        .expect(200);
      
      expect(Array.isArray(response.body)).toBe(true);
      expect(response.body.length).toBeGreaterThan(0);
    });
    
    test('should get single post by ID', async () => {
      const response = await request(baseURL)
        .get('/posts/1')
        .expect(200);
      
      expect(response.body).toHaveProperty('id', 1);
      expect(response.body).toHaveProperty('userId');
      expect(response.body).toHaveProperty('title');
      expect(response.body).toHaveProperty('body');
    });
    
    test('should return 404 for non-existent post', async () => {
      const response = await request(baseURL)
        .get('/posts/99999')
        .expect(404);
    });
    
  });
  
  // ========================================
  // POST REQUEST EXAMPLES
  // ========================================
  
  describe('POST Requests', () => {
    
    test('should create new post', async () => {
      const newPost = {
        title: 'Test Post Title',
        body: 'This is test post body content',
        userId: 1
      };
      
      const response = await request(baseURL)
        .post('/posts')
        .send(newPost)
        .expect(201);
      
      expect(response.body).toHaveProperty('id');
      expect(response.body.title).toBe(newPost.title);
      expect(response.body.body).toBe(newPost.body);
      expect(response.body.userId).toBe(newPost.userId);
    });
    
    test('should create post with custom headers', async () => {
      const newPost = {
        title: 'Post with headers',
        body: 'Content',
        userId: 1
      };
      
      const response = await request(baseURL)
        .post('/posts')
        .set('Content-Type', 'application/json')
        .set('Accept', 'application/json')
        .send(newPost)
        .expect(201);
      
      expect(response.body).toHaveProperty('id');
    });
    
  });
  
  // ========================================
  // PUT REQUEST EXAMPLES
  // ========================================
  
  describe('PUT Requests', () => {
    
    test('should update existing post', async () => {
      const updatedPost = {
        id: 1,
        title: 'Updated Title',
        body: 'Updated body content',
        userId: 1
      };
      
      const response = await request(baseURL)
        .put('/posts/1')
        .send(updatedPost)
        .expect(200);
      
      expect(response.body.id).toBe(1);
      expect(response.body.title).toBe(updatedPost.title);
      expect(response.body.body).toBe(updatedPost.body);
    });
    
  });
  
  // ========================================
  // PATCH REQUEST EXAMPLES
  // ========================================
  
  describe('PATCH Requests', () => {
    
    test('should partially update post', async () => {
      const partialUpdate = {
        title: 'Partially Updated Title'
      };
      
      const response = await request(baseURL)
        .patch('/posts/1')
        .send(partialUpdate)
        .expect(200);
      
      expect(response.body.title).toBe(partialUpdate.title);
      expect(response.body).toHaveProperty('body'); // Останалите полета остават
    });
    
  });
  
  // ========================================
  // DELETE REQUEST EXAMPLES
  // ========================================
  
  describe('DELETE Requests', () => {
    
    test('should delete post', async () => {
      await request(baseURL)
        .delete('/posts/1')
        .expect(200);
    });
    
  });
  
  // ========================================
  // QUERY PARAMETERS EXAMPLES
  // ========================================
  
  describe('Query Parameters', () => {
    
    test('should filter posts by userId', async () => {
      const response = await request(baseURL)
        .get('/posts')
        .query({ userId: 1 })
        .expect(200);
      
      expect(Array.isArray(response.body)).toBe(true);
      response.body.forEach(post => {
        expect(post.userId).toBe(1);
      });
    });
    
    test('should support multiple query parameters', async () => {
      const response = await request(baseURL)
        .get('/comments')
        .query({ postId: 1 })
        .query({ email: 'Eliseo@gardner.biz' })
        .expect(200);
      
      expect(Array.isArray(response.body)).toBe(true);
    });
    
  });
  
  // ========================================
  // RESPONSE VALIDATION EXAMPLES
  // ========================================
  
  describe('Response Validation', () => {
    
    test('should validate response structure', async () => {
      const response = await request(baseURL)
        .get('/posts/1')
        .expect(200)
        .expect('Content-Type', /json/);
      
      const post = response.body;
      
      // Проверка на типовете
      expect(typeof post.id).toBe('number');
      expect(typeof post.userId).toBe('number');
      expect(typeof post.title).toBe('string');
      expect(typeof post.body).toBe('string');
      
      // Проверка на стойностите
      expect(post.title.length).toBeGreaterThan(0);
      expect(post.body.length).toBeGreaterThan(0);
    });
    
    test('should validate array response', async () => {
      const response = await request(baseURL)
        .get('/posts')
        .expect(200);
      
      expect(Array.isArray(response.body)).toBe(true);
      expect(response.body.length).toBe(100);
      
      // Проверка на първия елемент
      const firstPost = response.body[0];
      expect(firstPost).toHaveProperty('id');
      expect(firstPost).toHaveProperty('userId');
      expect(firstPost).toHaveProperty('title');
      expect(firstPost).toHaveProperty('body');
    });
    
  });
  
});


blacklist.txt
# These are comments that are ignored
# Each line is a path to be skipped

# Directories to be skipped recursively
node_modules
.git
.vscode
.husky
.gitignore
dist
build
coverage
docs


# Specific files to be skipped
LICENSE
package-lock.json
yarn.lock


